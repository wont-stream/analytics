declare const _exports: {
  BMW: {
    regex: string;
    device: string;
    models: {
      regex: string;
      model: string;
    }[];
  };
  Jeep: {
    regex: string;
    device: string;
    models: {
      regex: string;
      model: string;
    }[];
  };
  Tesla: {
    regex: string;
    device: string;
    models: {
      regex: string;
      model: string;
    }[];
  };
  "MAC AUDIO": {
    regex: string;
    device: string;
    models: {
      regex: string;
      model: string;
    }[];
  };
  Topway: {
    regex: string;
    device: string;
    models: {
      regex: string;
      model: string;
    }[];
  };
};
export = _exports;
